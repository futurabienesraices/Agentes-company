# Airtable personal access token with read/write access to FBR_CRM_Master
AIRTABLE_API_TOKEN=

# FBR_CRM_Master base ID. The current production base is preconfigured in code,
# but keeping this variable allows safe migration to another base later.
AIRTABLE_BASE_ID=app7dn7435WA9fa7R

# Long random value required in the x-matching-secret request header.
MATCHING_API_SECRET=
