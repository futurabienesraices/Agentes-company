import Link from "next/link";
import RecordAction from "../components/RecordAction";
import { getPublicationData } from "../../lib/operations";

export default async function PublicacionesPage() {
  const data = await getPublicationData();
  return <main><nav className="agentNav"><Link href="/">← Centro de operaciones</Link><Link href="/captador">Agente Captador</Link></nav><header className="agentHero"><div><p className="eyebrow">AGENTE DE PUBLICACIONES</p><h1>Contenido listo para salir</h1><p className="lead">Mueve cada pieza por su flujo editorial directamente en Airtable.</p></div><span className={`systemStatus ${data.connected ? "online" : "offline"}`}><span className="dot" />{data.connected ? `${data.total} publicaciones` : "Sin conexión"}</span></header><section className="panel commercialPanel"><div className="panelHeader"><div><p className="eyebrow">COLA EDITORIAL</p><h2>Publicaciones en proceso</h2></div><span className="counter">{data.items.length}</span></div><div className="commercialQueue">{data.items.length ? data.items.map((item, index) => <article className="matchCard" key={item.id}><div className="matchRank">{index + 1}</div><div className="matchContent"><div className="matchTitle"><strong>{item.name}</strong><span>{item.createdAt || "Sin fecha"} · {item.status}</span></div><p>Prepara el material y marca la pieza cuando esté lista o publicada.</p><div className="recordActions"><RecordAction recordId={item.id} entity="publication" label="Marcar lista" fields={{ status: "Lista" }} successMessage="Publicación lista" /><RecordAction recordId={item.id} entity="publication" label="Marcar publicada" fields={{ status: "Publicada", date: new Date().toISOString().slice(0, 10) }} successMessage="Publicación completada" secondary /></div></div></article>) : <p className="emptyState">No hay publicaciones pendientes.</p>}</div></section></main>;
}
